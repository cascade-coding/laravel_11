-- ! NOT NULL Constraint
-- * By default, a column can hold NULL values.

-- * The NOT NULL constraint enforces a column to NOT accept NULL values.

-- ! Example
-- * CREATE TABLE employees (name VARCHAR(120) NOT NULL, email VARCHAR(255) NOT NULL, age INT);


